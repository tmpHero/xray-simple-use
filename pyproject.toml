[project]
name = "xray-simple-use"
version = "0.1.0"
description = "Lightweight Xray-core deployment tool for Linux"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
xray-simple = "xray_simple_use.main:main"
